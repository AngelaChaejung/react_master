import React, { useContext } from "react";
import { Context1 } from "./Home";

const Detail = () => {
  const schedule = useContext(Context1);
  return <div>디테일페이지지롱</div>;
};

export default Detail;
